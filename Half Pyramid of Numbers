#include <stdio.h>
int main() {
   int i, j, ro;
   printf("Enter the number of rows: ");
   scanf("%d", &ro);
   for (i = 1; i <= ro; ++i) {
      for (j = 1; j <= i; ++j) {
         printf("%d ", j);
      }
      printf("\n");
   }
   return 0;
}
